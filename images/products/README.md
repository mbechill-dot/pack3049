# product photos

Drop popcorn product photos here using the exact filenames listed in the main README.
Square images, about 800 pixels wide. The popcorn page picks them up automatically
and falls back to a colored tile if a file is missing.
